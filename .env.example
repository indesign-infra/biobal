# ── Turso (base de datos) ─────────────────────────────────────────────
# Copiá este archivo a .env.local y completá los valores.
PRIVATE_TURSO_DATABASE_URL=
PRIVATE_TURSO_AUTH_TOKEN=

# ── OpenAI (chat IA) — opcional; vacío usa el fallback por palabras clave
OPENAI_API_KEY=

# ── Behold.so (feed de Instagram) — opcional
BEHOLD_FEED_URL=

# ── Secreto para el cron de Instagram — opcional
CRON_SECRET=
